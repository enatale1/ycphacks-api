class HardwareImage {
    constructor(
        id,
        imageUrl,
        hardwareId
    ) {
        this.id = id
        this.imageUrl = imageUrl
        this.hardwareId = hardwareId
    }
}

module.exports = HardwareImage;